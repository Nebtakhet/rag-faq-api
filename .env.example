# Copy this file to .env and replace placeholder values.

# Required for embeddings and answer generation.
OPENAI_API_KEY=sk-your-real-key

# Required for /admin/* endpoints.
ADMIN_API_KEY=my-admin-secret

# Optional upload limit per file in bytes (default: 5242880 = 5MB).
MAX_UPLOAD_BYTES=5242880

# Optional ingestion quality tuning.
# INGESTION_CHUNK_SIZE default: 1000
# INGESTION_CHUNK_OVERLAP default: 200
# INGESTION_MIN_ALPHA_RATIO default: 0.15
# INGESTION_* values are useful when documents are very noisy or very short.
# INGESTION_CHUNK_SIZE=1000
# INGESTION_CHUNK_OVERLAP=200
# INGESTION_MIN_ALPHA_RATIO=0.15

# Optional DB URL used by Alembic and docker-compose overrides.
# Local default fallback is defined in alembic.ini.
# SQLALCHEMY_DATABASE_URI=sqlite:///./app.db

# Optional cache URL used in docker-compose.
# Set this in production or multi-instance deployments so rate limiting is shared.
# REDIS_URL=redis://redis:6379/0

# Optional API rate limiting.
# ASK_RATE_LIMIT default: 300/minute
# ADMIN_RATE_LIMIT default: 120/minute
# ASK_RATE_LIMIT=300/minute
# ADMIN_RATE_LIMIT=120/minute
# RATE_LIMIT_TRUST_PROXY_HEADERS=false
# RATE_LIMIT_TRUSTED_PROXY_IPS=127.0.0.1

# Optional legacy keys used by CI migration env.
# SECRET_KEY=replace-with-long-random-secret
# REFRESH_TOKEN_SECRET=replace-with-long-random-secret
# AUTO_CREATE_SCHEMA=false
